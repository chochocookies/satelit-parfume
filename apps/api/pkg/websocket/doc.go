// Package websocket will carry order-status, inventory, and POS events
// over the /ws endpoint (introduced alongside the features that need it).
// Not implemented yet — scaffolded in Phase 1, built out in its own phase.
package websocket
